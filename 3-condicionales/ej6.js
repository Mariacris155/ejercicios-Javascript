let num = parseInt(window.prompt("introduce un numero:"))

num < 1
? console.log("no se calcula porque es negativo")
: console.log(`El cuadrado de $(num) es $(num**2) y el cubo es (num**3)`)

let cuadrado =num**2
let cubo =num**3 